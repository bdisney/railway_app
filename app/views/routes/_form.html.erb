<%= form_for(@route) do |f| %>
  <%= render 'shared/errors_messages', resource: @route %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

 <div class="actions">
    <%= f.submit %>
  </div>
<% end %>