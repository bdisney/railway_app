<% if resource_list.present? %>
    <ul>
      <% resource_list.each do |item| %>
          <li>
            <%= link_to item.read_attribute(attr), item %>
          </li>
      <% end %>
    </ul>
<% else %>
    <%= empty_text.present? ? empty_text : 'Empty' %>
<% end %> 