<table>
  <thead>
    <tr>
      <th><%= t('admin.railway_stations.attributes.title') %></th>
      <th><%= t('admin.railway_stations.attributes.position') %></th>
    </tr>
  </thead>
  <tbody>
    <% stations.each do |station| %>
      <tr>
        <td><%= link_to station.title, admin_railway_station_path(station) %></td>
        <td>
          <%= form_tag(update_station_place_in_route_admin_railway_station_path(station), method: :patch) do %>
          <%= hidden_field_tag :route_id, @route.id %>
              
          <%= number_field_tag :position, station.within_route(@route, :position) %>
          <%= time_field_tag :arrival_time, station.within_route(@route, :arrival_time), placeholder: t('admin.railway_stations.attributes.arrival_time') %>
          <%= time_field_tag :departure_time, station.within_route(@route, :departure_time), placeholder: t('admin.railway_stations.attributes.departure_time') %>
          <%= submit_tag t('shared.update'), class: '' %>
          
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table> 


<!-- Horizontal Steppers -->
<div class="row">
    <div class="col-md-12">
        <!-- Stepers Wrapper -->
        <ul class="stepper stepper-horizontal">
        <% stations.each do |station| %>
        <li class="completed">
        <a href="<%= admin_railway_station_path(station) %>">
                    <span class="circle" ><%= station.within_route(@route, :position) %></span>
                    <span class="label"><%=  station.title %></span>
                </a>
            </li>
            <% end %>
        </ul>
        <!-- /.Stepers Wrapper -->
    </div>
</div>
<!-- /.Horizontal Steppers -->






