<div class="divider-new">
    <h2 class="h2-responsive">
        <%= t('.title') %><strong> <span class="text-muted"></span></strong>
    </h2>
</div>

<%= render 'form', carriage: @carriage %>
<br/>
<%= link_to  t('shared.back'), admin_train_carriages_path, class: "text-danger" %>